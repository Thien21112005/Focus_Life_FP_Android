<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/mainContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toTopOf="@+id/navShell"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <HorizontalScrollView
        android:id="@+id/navShell"
        android:layout_width="0dp"
        android:layout_height="92dp"
        android:background="@drawable/bg_bottom_nav"
        android:fillViewport="false"
        android:overScrollMode="never"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:scrollbars="none"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <LinearLayout
            android:id="@+id/bottomNav"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingTop="12dp"
            android:paddingBottom="12dp">

            <TextView android:id="@+id/tvNavHome" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_home" />
            <TextView android:id="@+id/tvNavHealth" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_health" />
            <TextView android:id="@+id/tvNavFocus" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_focus" />
            <TextView android:id="@+id/tvNavMap" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_map" />
            <TextView android:id="@+id/tvNavDiary" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_diary" />
            <TextView android:id="@+id/tvNavAiCoach" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_ai_coach" />
            <TextView android:id="@+id/tvNavNotification" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_notification" />
            <TextView android:id="@+id/tvNavReport" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_report" />
            <TextView android:id="@+id/tvNavStatistic" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_statistic" />
            <TextView android:id="@+id/tvNavTarget" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_target" />
            <TextView android:id="@+id/tvNavProfile" style="@style/FocusLife.BottomNavItem" android:text="@string/nav_profile" />
        </LinearLayout>
    </HorizontalScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
